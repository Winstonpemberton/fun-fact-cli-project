#!/usr/bin/env ruby

require "bundler/setup"
require "./config/environment"
#require "cli_project"

require "pry"
puts "Please wait, loading facts...."
sleep 3
puts "I swear it's not frozen, takes a solid minute"
Menu.start

require "irb"
#IRB.start(__FILE__)
