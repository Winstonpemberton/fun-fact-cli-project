#!/usr/bin/env ruby

require "bundler/setup"
require "./config/environment"
#require "cli_project"

require "pry"
puts "Please wait, loading facts...."
sleep 5
puts "I swear it's not frozen"
Menu.start

require "irb"
#IRB.start(__FILE__)
